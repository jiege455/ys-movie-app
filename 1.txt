请为我设计一个基于苹果CMS V10 + Flutter的无支付影视APP项目，包含以下模块：  
1. 苹果CMS后台插件：管理APP配置（名称/LOGO/注册方式）、广告（banner/插屏）、注册规则（第三方登录）、搜索索引（Elasticsearch管理）、版本管理、公告、求片/反馈、消息中心、分享统计、清理缓存；  
2. Flutter APP：首页（分类/推荐）、播放页（视频播放/弹幕/画中画）、个人中心（检查升级/公告/求片/反馈/消息中心/分享/清理缓存/设置）；  
3. 数据库：苹果CMS共用MySQL，插件独立表存储广告/注册规则/版本/公告/求片/反馈/消息/分享数据；  
4. 技术栈：苹果CMS V10（PHP）、Flutter 3.16+（Dart）、MySQL、Redis（缓存）、Elasticsearch（搜索）。  

请生成：  
- 项目目录结构（苹果CMS插件目录 + Flutter项目目录）；  
- Flutter的pubspec.yaml依赖配置（含视频播放、弹幕、网络请求、状态管理、画中画、搜索、分享相关库）；  
- 苹果CMS插件的plugin.json元数据文件（名称、版本、作者、安装/卸载脚本路径）。  


### 一、苹果CMS后台插件开发提示词  
请为苹果CMS V10开发一个名为“AppManager”的插件，实现以下功能：  


#### 1. 基础结构与菜单  
- 插件目录结构：包含controller/admin、view/admin、config.php、hook.php、install.php、uninstall.php；  
- 后台菜单：在苹果CMS后台“系统设置”下添加“APP管理”子菜单，包含子菜单项：“基础配置”“广告管理”“注册规则”“搜索索引”“版本管理”“公告管理”“用户诉求”“消息中心”“分享统计”；  
- 基础配置：读取/写入苹果CMS系统配置表（system_config），存储APP名称、LOGO URL、注册方式（手机号/微信/Apple ID）。  

请生成：  
- plugin.json（含插件名称、版本、作者、安装/卸载脚本路径）；  
- hook.php（注册“admin_menu”钩子，添加“APP管理”主菜单及子菜单）；  
- Config.php控制器（index()读取配置，save()保存配置到system_config表）。  


#### 2. 广告管理功能  
- 存储广告数据到apple_ad表（字段：id、position、title、image、link、status、create_time）；  
- 后台功能：列表展示（支持按位置筛选）、添加广告（含图片上传）、修改状态（启用/禁用）。  

请生成：  
- Ad.php控制器（index()/add()/status()方法）；  
- ad.html视图（表格展示广告列表，表单用于添加广告，使用Bootstrap栅格布局）；  
- SQL建表语句（apple_ad表，含索引idx_position优化查询）。  


#### 3. 注册规则管理  
- 存储配置到apple_register_rule表（字段：id、register_type、wechat_appid、wechat_secret、apple_login_enabled）；  
- 后台功能：表单配置（注册方式单选、微信AppID/Secret输入、Apple ID登录开关）、保存配置（仅一条记录，id=1）。  

请生成：  
- Register.php控制器（index()/save()方法，含表单验证）；  
- register.html视图（表单展示配置项）；  
- 默认数据：install.php中插入初始配置（register_type=0）。  


#### 4. 搜索索引管理（Elasticsearch集成）  
- 功能：监控Elasticsearch索引状态、重建索引、管理同义词库；  
- 数据同步：通过Logstash或Python脚本将苹果CMS影视数据实时同步到Elasticsearch（基于update_time字段增量同步）；  
- 后台界面：展示索引文档数、节点状态、CPU/内存占用；提供“一键重建索引”按钮和同义词编辑表单。  

请生成：  
- 后台“搜索索引管理”页面的HTML模板（使用Bootstrap样式）；  
- 控制器方法（index()展示状态、rebuild()触发重建、synonym()编辑同义词）；  
- 调用Elasticsearch API的工具类（如获取索引状态、更新同义词）。  


#### 5. 版本管理（检查升级）  
- 数据库设计：apple_version表（字段：id、version_code、version_name、update_log、release_time、is_force_update）；  
- 后台功能：添加/编辑版本信息（如v1.0.1更新日志：“修复播放卡顿问题”）；  
- API接口：GET /api/app/version，返回最新版本号、更新日志、是否强制更新。  

请生成：  
- apple_version表SQL建表语句（含索引idx_release_time）；  
- Version.php控制器（index()读取最新版本，save()添加/编辑版本）；  
- 版本管理后台页面（表单含版本号/名称/日志/发布时间输入）。  


#### 6. 公告管理  
- 数据库设计：apple_announcement表（字段：id、title、content、is_show、create_time、sort_order）；  
- 后台功能：发布公告（标题/内容/排序）、批量上下架（is_show=1显示，0隐藏）；  
- API接口：GET /api/app/announcements，返回所有显示中的公告（按sort_order排序）。  

请生成：  
- apple_announcement表SQL建表语句；  
- Announcement.php控制器（index()列表展示，save()保存公告，toggle()切换显示状态）；  
- 公告管理后台页面（表格展示公告列表，含上下架/排序/删除按钮）。  


#### 7. 用户诉求管理（求片/反馈）  
- 数据库设计：  
  - 求片表apple_request：id、user_id、vod_name、vod_desc、status（0待处理/1已回复）、create_time；  
  - 反馈表apple_feedback：id、user_id、content、contact（联系方式）、status（0待处理/1已回复）、create_time；  
- 后台功能：  
  - 求片列表：展示用户求片的影视名称/描述/状态，支持回复；  
  - 反馈列表：展示用户反馈内容/联系方式/状态，支持回复；  
- API接口：  
  - POST /api/user/request：提交求片；  
  - POST /api/user/feedback：提交反馈；  
  - GET /api/user/requests：获取用户个人求片历史；  
  - GET /api/user/feedbacks：获取用户个人反馈历史。  

请生成：  
- apple_request/apple_feedback表SQL建表语句；  
- Request.php/Feedback.php控制器（含用户提交接口、后台管理接口）；  
- 后台求片/反馈管理页面（列表+回复表单）。  


#### 8. 消息中心  
- 数据库设计：apple_message表（字段：id、user_id、title、content、is_read、create_time、type（0升级/1公告/2回复））；  
- 功能：  
  - 消息类型：升级提示、公告通知、求片/反馈回复通知；  
  - API接口：  
    - GET /api/app/messages：获取用户未读/已读消息列表；  
    - POST /api/app/message/read：标记消息为已读；  
  - 后台管理：发送系统消息（选择用户群体/消息类型/内容）。  

请生成：  
- apple_message表SQL建表语句；  
- Message.php控制器（index()消息列表，read()标记已读，send()后台发送消息）；  
- 消息中心后台页面（表单选择用户/类型/内容，发送消息）。  


#### 9. 分享统计  
- 数据库设计：apple_share表（字段：id、user_id、share_type（0分享APP/1分享影视）、share_target（影视ID/空）、create_time）；  
- 功能：  
  - API接口：POST /api/app/share，记录用户分享行为；  
  - 统计接口：GET /api/app/share/stats，返回分享总量/今日分享量（按类型分组）。  

请生成：  
- apple_share表SQL建表语句；  
- Share.php控制器（save()记录分享，stats()统计分享数据）。  


#### 10. 清理缓存  
- 功能：提供API接口触发前端缓存清理（或前端直接调用本地缓存清理方法）；  
- 后台管理：展示缓存清理次数统计（可选，需前端上报）。  

请生成：  
- 清理缓存API接口（GET /api/app/clear_cache，返回清理成功提示）；  
- 后台缓存统计页面（展示清理次数趋势图，可选）。  


### 二、Flutter APP开发提示词（含清理缓存、个人中心全功能）  


#### 1. 基础配置与依赖  
生成pubspec.yaml依赖配置，包含：  
- 视频播放：video_player、chewie；  
- 弹幕：danmaku_flame_widget；  
- 网络请求：dio、cached_network_image；  
- 状态管理：GetX；  
- 画中画：pip_mode；  
- 搜索：elasticsearch_dart；  
- 分享：share_plus；  
- 工具类：crypto、flutter_cupertino_date_picker。  

并生成Flutter项目基础目录结构（lib/main.dart、models、services、views、widgets）。  


#### 2. 首页（分类/推荐）  
开发首页，包含：  
- 顶部轮播图（从/api/banner/list获取数据，cached_network_image缓存）；  
- 分类导航（从/api/vod/class/list获取，横向滚动）；  
- 推荐列表（从/api/vod/hot获取，ListView.builder展示）。  

要求：用GetX管理状态，Dio封装网络请求（baseURL=https://api.example.com）。  


#### 3. 播放页（视频播放/弹幕/画中画）  
开发播放页，包含：  
1. 视频播放器：video_player + chewie实现，支持播放/暂停、进度条、全屏；  
2. 弹幕：danmaku_flame_widget，从wss://api.example.com/danmaku/vodId获取实时弹幕；  
3. 画中画功能：  
   - 页面右下角显示PiP按钮（仅在非PiP状态显示）；  
   - 点击按钮调用原生PiP API（iOS用AVPictureInPictureController，Android用PictureInPictureParams）；  
4. 清晰度切换：调用/api/vod/play获取多分辨率地址，切换视频源。  

要求：  
- 播放器初始化显示CircularProgressIndicator；  
- 弹幕颜色随机、字体24、透明度0.8；  
- dispose()中释放播放器和弹幕控制器。  


#### 4. 个人中心（全功能）  
开发个人中心页面，包含以下功能：  


##### （1）检查升级与弹窗提示  
- 定时任务：每天1次调用/api/app/version接口；  
- 版本对比：本地版本号与API返回的version_code对比；  
- 弹窗提示：  
  - 有更新时弹出AlertDialog，显示版本名称、更新日志、“立即更新”按钮（跳转应用商店）；  
  - 强制更新时弹窗不可关闭。  

请生成：  
- 版本检查工具类VersionChecker（含定时任务、API调用、弹窗逻辑）；  
- 升级弹窗Widget（自定义AlertDialog样式）。  


##### （2）公告展示  
- 个人中心“公告”入口：跳转AnnouncementListPage；  
- 公告列表页：ListView.builder展示公告标题+发布时间，未读公告标红圆点；  
- 公告详情页：显示标题+富文本内容，点击返回列表页标记为已读（调用/api/app/message/read）。  

请生成：  
- AnnouncementListPage/AnnouncementDetailPage页面；  
- 公告数据模型Announcement（含id/title/content/is_read）。  


##### （3）求片/反馈提交与历史  
- 求片页：表单含影视名称（TextField）、简介（TextField），“提交求片”按钮（调用POST /api/user/request）；  
- 反馈页：表单含反馈内容（TextField）、联系方式（TextField），“提交反馈”按钮（调用POST /api/user/feedback）；  
- 历史页面：  
  - 求片历史：展示用户提交的求片记录（状态：待处理/已回复）；  
  - 反馈历史：展示反馈记录（状态同上）。  

请生成：  
- RequestPage/FeedbackPage提交表单；  
- RequestHistoryPage/FeedbackHistoryPage历史列表（含状态标签）。  


##### （4）消息中心  
- 个人中心“消息中心”入口：跳转MessageListPage；  
- 消息列表页：展示消息标题+时间，未读消息标红角标；  
- 消息详情页：显示标题+内容，“标记已读”按钮（调用POST /api/app/message/read）；  
- 消息类型区分：升级提示/公告通知/回复通知（不同图标）。  

请生成：  
- MessageListPage/MessageDetailPage页面；  
- 消息数据模型Message（含id/title/content/is_read/type）。  


##### （5）分享功能  
- 个人中心“分享”入口：调用share_plus插件分享APP；  
- 分享内容：APP名称+下载链接；  
- 分享统计：调用POST /api/app/share记录分享行为。  

请生成：  
- 分享按钮点击事件（使用share_plus插件）；  
- 分享成功提示SnackBar。  


##### （6）清理缓存  
- 个人中心“设置”入口：添加“清理缓存”按钮；  
- 功能：调用imageCache.clear()清理本地图片缓存，调用Dio清除网络缓存（可选）；  
- 提示：清理完成后显示SnackBar“缓存已清理”。  

请生成：  
- 清理缓存按钮点击事件；  
- 缓存清理逻辑（清除imageCache和Dio缓存）。  


#### 5. 网络请求与API对接  
封装Dio工具类ApiService，对接以下接口：  
- GET /api/banner/list（轮播图）；  
- GET /api/vod/class/list（分类）；  
- GET /api/vod/hot（热门影视）；  
- GET /api/vod/play（播放地址）；  
- GET /api/user/history（观看历史）；  
- GET /api/user/favorite（收藏列表）；  
- GET /api/search（Elasticsearch搜索）；  
- GET /api/app/version（版本检查）；  
- GET /api/app/announcements（公告列表）；  
- POST /api/user/request（提交求片）；  
- POST /api/user/feedback（提交反馈）；  
- GET /api/app/messages（消息列表）；  
- POST /api/app/message/read（标记消息已读）；  
- POST /api/app/share（分享统计）；  
- GET /api/app/clear_cache（清理缓存）。  

响应处理：code=1为成功，否则抛异常。  


### 三、数据库设计提示词  
生成苹果CMS插件新增表SQL：  
1. 版本表apple_version：id、version_code、version_name、update_log、release_time、is_force_update；  
2. 公告表apple_announcement：id、title、content、is_show、create_time、sort_order；  
3. 求片表apple_request：id、user_id、vod_name、vod_desc、status、create_time；  
4. 反馈表apple_feedback：id、user_id、content、contact、status、create_time；  
5. 消息表apple_message：id、user_id、title、content、is_read、create_time、type；  
6. 分享表apple_share：id、user_id、share_type、share_target、create_time。  


### 四、编译与平台配置提示词  
指导Flutter APP编译与平台配置：  
1. 生成pubspec.lock：flutter pub get；  
2. 编译安卓APK：flutter build apk --split-per-abi；  
3. 编译iOS IPA：flutter build ios；  
4. iOS隐私权限：Info.plist添加网络、相册、麦克风权限；  
5. 安卓权限：AndroidManifest.xml添加网络、存储、前台服务权限。  


### 五、调试与优化提示词  
当我遇到以下问题时，请给出解决方案：  
1. 版本检查定时任务不执行；  
2. 公告列表加载慢；  
3. 消息中心未读标记不更新；  
4. 分享功能在iOS审核被拒；  
5. 清理缓存后图片仍显示旧数据。  

并提供以下优化建议：  
1. 首页加载慢；  
2. 播放页内存占用高；  
3. 观看历史列表滑动卡顿；  
4. 安卓APK体积过大。  


### 使用说明  
1. 按模块顺序复制提示词到AI工具（如Kiro），先生成项目框架，再开发插件、APP页面、数据库；  
2. 人工补全业务细节（如用户注册校验、广告图片压缩）；  
3. 测试功能，用提示词解决问题，用优化建议提升性能。  


此提示词覆盖**全项目所有需求**，包含清理缓存、个人中心全功能、后台管理，复制后可直接用于AI生成代码，新手也能快速落地。